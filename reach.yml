---
- name: Reachability check (SSH port)
  hosts: all
  gather_facts: false
  tasks:
    - name: SSH port 22
      ansible.builtin.wait_for:
        host: "{{ ansible_host }}"
        port: 22
        timeout: 5
      delegate_to: localhost
      ignore_errors: true
